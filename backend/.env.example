# Used by `prisma migrate` and `prisma generate`.
# Use a direct (non-pooling) Postgres URL here. The Worker itself reads
# DATABASE_URL from wrangler.toml [vars] (typically a Prisma Accelerate URL),
# not this file.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
